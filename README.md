# Voice Recogination

### **Run Backend**

```
cd ./backend
pip install -r requirements.txt             (first time only)
flask run --reload
```

### **Run Frontend**

```
cd ./client
npm install                                 (first time only)
npm start
```
